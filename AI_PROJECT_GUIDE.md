# AI 项目指南 - NoteSpace

> 专为 AI 助手设计的项目指南，包含完整的技术细节、代码规范和架构说明，便于 AI 快速理解和跟进项目开发。

---

## 项目概述

**项目名称**: NoteSpace
**项目类型**: Vue 3 单页应用（SPA）
**核心功能**: 个人历程管理 + 富文本笔记系统 + 相册功能
**设计风格**: 浅色 Apple 风格 + 毛玻璃效果 + 线条装饰

### 技术栈总览

```yaml
框架: Vue 3.4+ (Composition API + <script setup>)
路由: Vue Router 4.2+ (History 模式)
UI库: Element Plus 2.5+
编辑器: WangEditor 5.x + @wangeditor/plugin-md
构建: Vite 5.0+
包管理: npm
```

---

## 目录结构详解

```
D:\header\personnal_introduce\
├── src/
│   ├── api/
│   │   └── login.js              # 登录 API 模拟接口
│   ├── router/
│   │   └── index.js              # 路由配置 + beforeEach 守卫
│   ├── views/
│   │   ├── Login.vue            # 登录页 (Apple 风格 + Logo)
│   │   ├── Home.vue             # 首页 (时间轴 + 富文本编辑)
│   │   ├── Notes.vue            # 笔记页 (富文本 + Medium 风格阅读)
│   │   └── Gallery.vue          # 相册页 (杂志风格布局 + 全屏灯箱)
│   ├── App.vue                  # 根组件 (全局样式 + 滚动条)
│   └── main.js                  # 应用入口
├── index.html                    # HTML 模板
├── vite.config.js               # Vite 配置 (端口 3000, 自动打开)
├── package.json                 # 依赖配置
├── README.md                    # 用户文档
└── AI_PROJECT_GUIDE.md          # 本文档
```

---

## 路由配置

### 路由表

| 路径 | 组件 | 认证 | 说明 |
|------|------|------|------|
| `/` | - | - | 重定向到 `/login` |
| `/login` | Login.vue | 否 | 登录页，已登录则跳转首页 |
| `/home` | Home.vue | 是 | 首页，历程时间轴 |
| `/notes` | Notes.vue | 是 | 笔记管理页 |
| `/gallery` | Gallery.vue | 是 | 相册页 |

---

## 组件架构

### 1. Login.vue - 登录页

**关键元素**:
- 浅色网格背景 + 3 个浮动动画圆球（低透明度）
- Logo 图标（渐变紫色星标）
- 毛玻璃卡片容器
- 表单验证规则
- 记住我功能 (localStorage 存储用户名)

**验证规则**:
- 用户名: 3-20 字符
- 密码: 6-20 字符

### 2. Home.vue - 首页

**布局结构**:
```
┌─────────────────────────────────────┐
│  Apple 风格导航栏                    │
├─────────────────────────────────────┤
│  英雄区域 (欢迎标题 + 统计卡片)       │
├─────────────────────────────────────┤
│  历程时间轴                          │
│   ┌────┐    ┌────┐    ┌────┐       │
│   │ L1 │    │ R1 │    │ L2 │       │
│   └────┘    └────┘    └────┘       │
└─────────────────────────────────────┘

侧边抽屉编辑器 (85% 宽度):
┌─────────────────────────────────────┐
│  ✏️ 添加经历    [取消] [保存]      │
├─────────────────────────────────────┤
│  标题输入框                          │
│  [日期选择] [标签选择]              │
├─────────────────────────────────────┤
│  WangEditor 工具栏                   │
├─────────────────────────────────────┤
│                                      │
│       富文本编辑区域                 │
│                                      │
└─────────────────────────────────────┘
```

**核心数据**:
- `experiences`: 历程数组 (localStorage: 'experiences')
- `currentTime`: 实时时间更新
- `editorRef`: WangEditor 实例引用

**方法列表**:
- `handleAddExperience()` - 打开侧边抽屉
- `handleSubmit()` - 提交新历程
- `handleDelete(id)` - 删除历程
- `updateTime()` - 更新当前时间

### 3. Notes.vue - 笔记页

**布局结构**:
```
┌─────────────────────────────────────┐
│  Apple 风格导航栏                    │
├─────────────────────────────────────┤
│  分类标签栏 (横向滚动)               │
├─────────────────────────────────────┤
│  笔记卡片网格 (响应式布局)           │
│   ┌──────┐  ┌──────┐  ┌──────┐    │
│   │ 卡片1│  │ 卡片2│  │ 卡片3│    │
│   └──────┘  └──────┘  └──────┘    │
└─────────────────────────────────────┘

侧边编辑抽屉 (85% 宽度):
┌─────────────────────────────────────┐
│  ✏️ 编辑笔记    [取消] [保存]       │
├─────────────────────────────────────┤
│  标题输入框 (无边框大字体)           │
│  [分类] [颜色选择器] [标签]         │
├─────────────────────────────────────┤
│  WangEditor 工具栏 (简洁版)          │
├─────────────────────────────────────┤
│                                      │
│       富文本编辑区域 (自适应)         │
│                                      │
└─────────────────────────────────────┘

Medium 风格全屏阅读:
┌─────────────────────────────────────┐
│                              [× 关闭] │
├─────────────────────────────────────┤
│  📷 学习  ·  2024年12月24日 周二     │
│                                      │
│  Vue 3 学习笔记                      │
│  #标签 #Vue #前端                     │
│                                      │
│  正文内容 (720px 宽度居中)           │
│  - 首段放大引导                       │
│  - Notion 风格排版                    │
│  - 优雅的代码高亮                     │
│                                      │
│        ─────── 🔖 ───────            │
└─────────────────────────────────────┘
```

**WangEditor 配置**:
```javascript
// 简洁工具栏
const toolbarConfig = {
  toolbarKeys: [
    'headerSelect', 'bold', 'italic', 'underline', 'through',
    'bulletedList', 'numberedList', 'todo',
    'fontSize', 'lineHeight', 'color', 'bgColor',
    'divider', 'undo', 'redo'
  ]
}
```

### 4. Gallery.vue - 相册页

**布局结构**:
```
┌─────────────────────────────────────┐
│  Apple 风格导航栏                    │
├─────────────────────────────────────┤
│  我的相册  [12 张照片]    [添加照片]  │
├─────────────────────────────────────┤
│  [全部] [生活] [旅行] [风景] [人物] [美食] │
├─────────────────────────────────────┤
│  杂志风格照片网格                     │
│   ┌────┐  ┌──────┐  ┌─┐  ┌────┐    │
│   │大图│  │中图  │  │小│  │    │    │
│   │2x2 │  │2x1   │  │图│  │中图│    │
│   └────┘  └──────┘  └─┘  └────┘    │
└─────────────────────────────────────┘

全屏灯箱:
┌─────────────────────────────────────┐
│ [×]                        [◀] [▶] │
│                                      │
│         ┌─────────────────┐         │
│         │                   │         │
│         │    照片大图       │         │
│         │                   │         │
│         └─────────────────┘         │
│                                      │
│  照片标题                              │
│  照片描述                              │
│  生活 · 2024年12月24日                 │
│                                      │
│  [缩1] [缩2] [缩3] ...                 │
└─────────────────────────────────────┘
```

**核心数据**:
```javascript
// 照片列表
const photos = ref([])  // localStorage: 'gallery_photos'

// 筛选
const selectedCategory = ref('全部')
const categories = ['生活', '旅行', '风景', '人物', '美食']

// 灯箱
const lightboxVisible = ref(false)
const currentIndex = ref(0)
```

---

## 数据结构规范

### 历程记录 (experience)
```javascript
{
  id: Number,          // 时间戳
  title: String,       // 标题
  date: String,        // YYYY-MM-DD 格式
  content: String,     // 富文本 HTML (新增)
  description: String, // 兼容旧数据
  tags: Array<String>, // 标签数组
  color: String        // 颜色值 #HEX
}
```

### 笔记记录 (note)
```javascript
{
  id: Number,              // 时间戳
  title: String,           // 标题
  category: String,        // 分类: 学习/工作/生活/想法/其他
  color: String,           // 颜色值 #HEX
  content: String,         // 富文本 HTML
  tags: Array<String>,     // 标签数组
  createdAt: String,       // ISO 8601 时间戳
  updatedAt: String        // ISO 8601 时间戳 (可选)
}
```

### 照片记录 (photo)
```javascript
{
  id: Number,          // 时间戳
  title: String,       // 照片标题
  description: String,  // 描述文本
  category: String,     // 分类: 生活/旅行/风景/人物/美食
  url: String,         // 图片 Base64 或 URL
  date: String         // ISO 8601 时间戳
}
```

---

## 设计规范

### 色彩系统 - 浅色 Apple 风格
```css
/* 品牌色 (渐变，用于装饰) */
--brand-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 主色调 */
--primary-color: #1d1d1f;         /* 主按钮/选中状态 */
--primary-hover: #000000;         /* 悬停深色 */

/* 背景色 - 浅色系 */
--bg-primary: #f5f5f7;            /* 主背景 */
--bg-white: #ffffff;              /* 卡片背景 */
--bg-elevated: #f9f9f9;           /* 抬升背景 */

/* 边框色 - 线条装饰 */
--border-light: #e5e5ea;          /* 浅色边框 */
--border-medium: #d1d1d6;         /* 中等边框 */
--border-dark: #c7c7cc;           /* 深色边框 */

/* 文字色 */
--text-primary: #1d1d1f;          /* 主要文字 */
--text-secondary: #6e6e73;        /* 次要文字 */
--text-tertiary: #86868b;         /* 辅助文字 */

/* 功能色 */
--accent-blue: #0071e3;
--accent-purple: #667eea;
--accent-green: #34c759;
--accent-red: #ff3b30;

/* 颜色选项 */
--color-blue: #409EFF;
--color-green: #67C23A;
--color-orange: #E6A23C;
--color-red: #F56C6C;
--color-gray: #909399;
--color-purple: #722ED1;
--color-cyan: #13C2C2;
--color-rose: #F5222D;
```

### 圆角规范
```css
--radius-sm: 8px;     /* 小元素/标签 */
--radius-md: 10px;    /* 按钮 */
--radius-lg: 12px;    /* 输入框/卡片 */
--radius-xl: 16px;    /* 大卡片 */
--radius-2xl: 20px;   /* 抽屉容器 */
--radius-full: 50%;   /* 圆形 */
```

### 导航栏规范 - Apple 风格
```css
.navbar {
  height: 64px;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20px) saturate(180%);
  border-bottom: 1px solid rgba(0, 0, 0, 0.08);
  box-shadow: 0 1px 0 rgba(0, 0, 0, 0.04);
}
```

### 按钮状态
```css
/* 选中状态 */
.category-tab.active,
.hero-btn.primary,
.add-btn {
  background: #1d1d1f;
  color: #fff;
}

/* 悬停效果 */
.button:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
}
```

### 毛玻璃效果
```css
.glass-card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(40px) saturate(180%);
  border: 1px solid rgba(0, 0, 0, 0.08);
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
}
```

---

## 代码规范

### 导入顺序
```javascript
// 1. Vue 核心
import { ref, reactive, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'

// 2. UI 组件库
import { ElMessage, ElMessageBox } from 'element-plus'
import { IconName1, IconName2 } from '@element-plus/icons-vue'

// 3. 项目模块
import { logoutApi } from '@/api/login'

// 4. 编辑器
import '@wangeditor/editor/dist/css/style.css'
import { Editor, Toolbar } from '@wangeditor/editor-for-vue'
```

---

## 已知问题和注意事项

### 1. WangEditor 依赖
- WangEditor 不支持 SSR（本项目无影响）

### 2. 样式深度选择器
```vue
<style scoped>
/* 使用 :deep() 修改子组件样式 */
.wrapper :deep(.child-class) {
  color: red;
}
</style>
```

### 3. 路由跳转
```javascript
// 使用 useRouter 而非 <router-link>
const router = useRouter()
router.push('/path')
```

---

## 页面功能清单

### 登录页
- [x] Apple 风格登录界面
- [x] 动态背景圆球动画
- [x] 表单验证
- [x] 记住我功能

### 首页
- [x] 时间轴展示历程
- [x] 富文本编辑器（侧边抽屉）
- [x] 统计卡片
- [x] 标签分类
- [x] 搜索功能
- [x] 增删改查

### 笔记页
- [x] 笔记网格展示
- [x] 分类筛选
- [x] 富文本编辑（侧边抽屉）
- [x] Medium/Notion 风格阅读
- [x] 全屏灯箱查看
- [x] 颜色标记
- [x] 标签系统
- [x] 增删改查

### 相册页
- [x] 杂志风格网格布局
- [x] 分类筛选
- [x] 照片上传
- [x] 全屏灯箱查看
- [x] 左右切换
- [x] 底部缩略图导航

---

## 快速命令参考

```bash
# 开发
npm run dev          # 启动开发服务器 (端口 3000)

# 构建
npm run build        # 生产构建
npm run preview      # 预览构建结果

# 依赖
npm install          # 安装依赖
npm install <pkg>    # 安装新包
```

---

**文档版本**: 2.0.0
**最后更新**: 2024-12-24
**维护者**: AI Assistant
